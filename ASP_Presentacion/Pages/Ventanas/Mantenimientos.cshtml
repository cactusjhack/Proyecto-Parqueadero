@page
@model ASP_Presentacion.Pages.Ventanas.MantenimientosModel
@{
    ViewData["Title"] = "Mantenimientos";
}

<center>
    <div>
        <form method="POST" enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Mantenimientos</h3>
            </div>

            <div>
                <input type="submit" value="Refrescar"
                       class="btn btn-secondary"
                       asp-page-handler="BtRefrescar" />
                @if (Model.Mantenimiento == null)
                {
                    <input type="submit" value="Nuevo"
                           class="btn btn-secondary"
                           asp-page-handler="BtNuevo" />
                }
                else
                {
                    <input type="submit" value="Guardar"
                           class="btn btn-secondary"
                           asp-page-handler="BtGuardar" />
                }
            </div>

            @if (Model.Mantenimiento != null)
            {
                <input type="hidden" asp-for="@Model.Mantenimiento!.Id" />
                <div>
                    <div class="form-group">
                        <label>Descripcion</label>
                        <input class="form-control" asp-for="@Model.Mantenimiento!.Descripcion" />
                    </div>
                    <div class="form-group">
                        <label>FechaInicio</label>
                        <input class="form-control" asp-for="@Model.Mantenimiento!.FechaInicio" />
                    </div>
                    <div class="form-group">
                        <label>FechaFin</label>
                        <input class="form-control" asp-for="@Model.Mantenimiento!.FechaFin" />
                    </div>
                    <div class="form-group">
                        <label>Activo</label>
                        <input asp-for="@Model.Mantenimiento!.Activo" />
                    </div>
                    <div class="form-group">
                        <label>Espacio</label>
                        <input class="form-control" asp-for="@Model.Mantenimiento!.Espacio" />
                    </div>
                    <div class="form-group">
                        <label>Empleado</label>
                        <input class="form-control" asp-for="@Model.Mantenimiento!.Empleado" />
                    </div>
                </div>
            }

            @if (Model.Mantenimiento != null && Model.Borrando)
            {
                <div class="popup">
                    <div>
                        <h2>Mensaje</h2>
                        <h6>¿Desea borrar el mantenimiento @Model.Mantenimiento!.Id?</h6>
                        <input type="submit" value="Cerrar"
                               asp-page-handler="BtCerrar"
                               class="btn btn-secondary" />
                        <input type="submit" value="Borrar"
                               asp-page-handler="BtBorrar"
                               class="btn btn-secondary" />
                    </div>
                </div>
            }

            @if (Model.Lista != null && Model.Lista.Count > 0)
            {
                <table class="table table-striped table-bordered table-hover">
                    <tr>
                        <th>Descripcion</th>
                        <th>FechaInicio</th>
                        <th>FechaFin</th>
                        <th>Activo</th>
                        <th>Espacio</th>
                        <th>Empleado</th>
                    </tr>
                    @foreach (var elemento in Model.Lista)
                    {
                        <tr>
                            <td>@elemento.Descripcion</td>
                            <td>@elemento.FechaInicio</td>
                            <td>@elemento.FechaFin</td>
                            <td>@elemento.Activo</td>
                            <td>@elemento.Espacio</td>
                            <td>@elemento.Empleado</td>
                            <td>
                                <input type="submit" value="Modificar"
                                       asp-page-handler="BtModificar"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                                <input type="submit" value="Borrar"
                                       asp-page-handler="BtBorrarVal"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                            </td>
                        </tr>
                    }
                </table>
            }
        </form>
    </div>
</center>